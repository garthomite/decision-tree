---
const steps = [
  {
    id: "welcome",
    title: "意思決定を\nする",
    description:
      "意思決定は難しいことがありますが、必ずしもそうである必要はありません。このガイドを使えば、自信を持って決められます。",
    detail:
      "Structure your thoughts into manageable layers. Reduce cognitive load by focusing on one variable at a time.",
    action: "はじめましょう！",
  },
  {
    id: "objective",
    eyebrow: "ステップ 1 / 5",
    title: "何を決めようとしていますか？",
    description:
      "これはどんな問いでもかまいません。たとえば:",
    detail: "",
    action: "次へ",
    examples: ["旅行に行くべき？", "転職するべき？", "引退できる？"],
    input: {
      label: "質問例",
      placeholder: "ここに質問を書いてください",
      value: "ガースと電話するべき？",
    },
  },
  {
    id: "gains",
    eyebrow: "ステップ 2 / 5",
    title: "何を得られますか？",
    description:
      "この決断を選んだ場合、何を得られますか？どんな利益がありますか？それはあなたにとって大切ですか？",
    detail: "",
    action: "次へ",
    input: {
      label: "得られるものの例",
      placeholder: "得られるものを書き出してください",
      value:
        "英語を練習できる\n英語は旅行に役立つ\n自信がつく\nガースにもっと気楽になれる\nガースをもっと人生に取り入れたいか分かる",
    },
  },
  {
    id: "stakes",
    eyebrow: "ステップ 3 / 5",
    title: "どんなリスクがありますか？",
    description:
      "何がうまくいかない可能性がありますか？考えられるリスクをここに書き出してください。",
    detail: "",
    action: "次へ",
    input: {
      label: "リスクの例",
      placeholder: "ここにリスクを書き出してください",
      value:
        "電話が得意ではない\nジェスチャーが使えない\n翻訳ツールが使えない\nいつ電話を切ればいいか分からない\n単純に恥ずかしく感じる",
    },
  },
  {
    id: "timeline",
    eyebrow: "ステップ 4 / 5",
    title: "では、これをしなかったら？",
    description:
      "この決断を実行しなかったら何が起こりますか？何を逃すことになりますか？",
    detail: "",
    action: "次へ",
    input: {
      label: "起こりうることの例",
      placeholder: "何もしなかった場合に起こることを書いてください",
      value:
        "ガースとつながる機会を逃す\nガースが悲しい、または傷ついたと感じるかもしれない\n英語を練習する機会を逃す\n自分のスキルを伸ばす機会を逃す",
    },
  },
  {
    id: "mitigation",
    eyebrow: "ステップ 5 / 5",
    title: "どうすればリスクを減らせますか？",
    description:
      "こうしたリスクを前提に、どうすれば最小限にできますか？リスクはいつでも軽減できます。",
    detail: "",
    action: "次へ",
    input: {
      label: "対策の例",
      placeholder: "リスクを減らす方法を書いてください",
      value:
        "恥ずかしいと感じていることを伝える\nジェスチャーのためにビデオ通話を使う\n同時にチャットで日本語を打つ\n「そろそろ行かないと」を覚える",
    },
  },
  {
    id: "conclusion",
    eyebrow: "結論",
    title: "結論",
    description:
      "得られるものに価値があるなら、あとはリスクを小さくしましょう。",
    detail:
      "Always remember the cost of not doing something or delaying something, it sometimes has a bigger impact of the risk itself.",
    action: "最初からやり直す",
  },
];
---

<section class="decision-app" data-decision-app data-steps={JSON.stringify(steps)}>
  <main class="app-main">
    <section class="screen screen-welcome is-active" data-screen="welcome" aria-live="polite">
      <div class="hero-mark"></div>
      <div class="hero-copy">
        <h1>意思決定を<br />する</h1>
        <p class="lede">
          意思決定は難しいことがありますが、必ずしもそうである必要はありません。このガイドを使えば、自信を持って決められます。
        </p>
        <p class="supporting-intro">
          このプロセスは、大きなことにも小さなことにも、ほとんどあらゆる決断に使えます。
        </p>
        <p class="supporting-intro">
          まずは一緒に例のシナリオを進めます。あとで同じプロセスを、自分自身の本当の決断にも使えるようになります。
        </p>
      </div>
    </section>

    <section class="screen screen-question" data-screen="question" hidden>
      <div class="screen-intro">
        <p class="eyebrow" data-eyebrow></p>
        <h2 data-title></h2>
        <p class="lede" data-description></p>
        <ul class="example-list" data-examples hidden></ul>
      </div>

      <div class="question-panel">
        <p class="supporting-copy" data-detail></p>
        <label class="editorial-input" data-input-wrap hidden>
          <span class="input-label" data-input-label></span>
          <div class="editorial-input-surface">
            <textarea
              rows="5"
              data-input
            ></textarea>
            <div class="editorial-input-accent" aria-hidden="true"></div>
          </div>
        </label>
        <div class="option-list" data-options></div>
      </div>
    </section>

    <section class="screen screen-summary" data-screen="summary" hidden>
      <div class="screen-intro">
        <p class="eyebrow">結論</p>
        <h2>結論</h2>
        <p class="lede">得られるものに価値があるなら、あとはリスクを小さくしましょう。</p>
      </div>

      <div class="summary-panel">
        <div class="summary-card">
          <span class="summary-label">これをしなかったら</span>
          <p data-summary-timeline></p>
        </div>
        <div class="summary-card">
          <span class="summary-label">リスク</span>
          <p data-summary-stakes></p>
        </div>
        <div class="summary-note summary-question">
          この二つのうち、より悪い結果はどちらですか？
        </div>
        <div class="summary-note">
          何かをしないことや先延ばしにすることの代償は、時にそのリスクそのものより大きな影響を持つことを忘れないでください。
        </div>
        <div class="summary-note">
          状況のリスクを減らせば減らすほど、自分の決断により安心感を持てるようになります。
        </div>
      </div>
    </section>
  </main>

  <div class="bottom-bar">
    <div class="bottom-shell">
      <button class="secondary-action" type="button" data-back hidden>戻る</button>
      <div class="progress" aria-label="進行状況" data-progress></div>
      <button class="primary-action" type="button" data-next>
        <span data-next-label>はじめましょう！</span>
        <span aria-hidden="true">→</span>
      </button>
    </div>
  </div>
</section>

<script is:inline>
  const root = document.querySelector("[data-decision-app]");

  if (root) {
    const steps = JSON.parse(root.dataset.steps || "[]");
    const questionScreen = root.querySelector('[data-screen="question"]');
    const summaryScreen = root.querySelector('[data-screen="summary"]');
    const welcomeScreen = root.querySelector('[data-screen="welcome"]');
    const nextButton = root.querySelector("[data-next]");
    const backButton = root.querySelector("[data-back]");
    const nextLabel = root.querySelector("[data-next-label]");
    const progress = root.querySelector("[data-progress]");
    const title = root.querySelector("[data-title]");
    const eyebrow = root.querySelector("[data-eyebrow]");
    const description = root.querySelector("[data-description]");
    const detail = root.querySelector("[data-detail]");
    const examplesHost = root.querySelector("[data-examples]");
    const inputWrap = root.querySelector("[data-input-wrap]");
    const inputLabel = root.querySelector("[data-input-label]");
    const inputField = root.querySelector("[data-input]");
    const optionsHost = root.querySelector("[data-options]");
    const questionPanel = root.querySelector(".question-panel");

    const state = {
      index: 0,
      selections: {},
    };

    for (const step of steps) {
      if (step.input?.value && !state.selections[step.id]) {
        state.selections[step.id] = step.input.value;
      }
    }

    function getStepIndexFromUrl() {
      const url = new URL(window.location.href);
      const stepId = url.searchParams.get("step");

      if (!stepId) return 0;

      const foundIndex = steps.findIndex((step) => step.id === stepId);
      return foundIndex >= 0 ? foundIndex : 0;
    }

    function syncUrl(replace = false) {
      const step = steps[state.index];

      if (!step) return;

      const url = new URL(window.location.href);

      if (step.id === "welcome") {
        url.searchParams.delete("step");
      } else {
        url.searchParams.set("step", step.id);
      }

      const nextUrl = `${url.pathname}${url.search}${url.hash}`;
      const method = replace ? "replaceState" : "pushState";
      window.history[method]({ step: step.id }, "", nextUrl);
    }

    const questionStepIds = steps
      .filter((step) => Array.isArray(step.options) || step.input)
      .map((step) => step.id);

    function renderProgress() {
      if (!progress) return;

      const dots = steps.map((step, index) => {
        const active = index === state.index ? "is-active" : "";
        const complete = index < state.index ? "is-complete" : "";
        return `<span class="progress-dot ${active} ${complete}" aria-hidden="true"></span>`;
      });

      progress.innerHTML = dots.join("");
    }

    function renderQuestion(step) {
      if (!questionScreen || !optionsHost || !title || !eyebrow || !description || !detail) {
        return;
      }

      eyebrow.textContent = step.eyebrow;
      title.textContent = step.title;
      description.textContent = step.description;
      detail.textContent = step.detail;

      if (examplesHost) {
        const hasExamples = Array.isArray(step.examples) && step.examples.length > 0;
        examplesHost.hidden = !hasExamples;
        examplesHost.innerHTML = hasExamples
          ? step.examples.map((example) => `<li>${example}</li>`).join("")
          : "";
      }

      if (inputWrap && inputField && inputLabel) {
        const hasInput = Boolean(step.input);
        inputWrap.hidden = !hasInput;
        questionPanel?.classList.toggle("question-panel-input", hasInput);

        if (hasInput) {
          inputLabel.textContent = step.input.label || "";
          inputField.placeholder = step.input.placeholder || "";
          inputField.value = state.selections[step.id] || step.input.value || "";
        } else {
          inputField.value = "";
        }
      }

      const selectedValue = state.selections[step.id];
      const hasOptions = Array.isArray(step.options) && step.options.length > 0;
      optionsHost.hidden = !hasOptions;
      optionsHost.innerHTML = (step.options || [])
        .map((option, index) => {
          const isSelected = selectedValue === option.label;
          return `
            <button
              class="option-card ${isSelected ? "is-selected" : ""}"
              type="button"
              data-option
              data-step="${step.id}"
              data-value="${option.label}"
              aria-pressed="${isSelected ? "true" : "false"}"
            >
              <span class="option-index">0${index + 1}</span>
              <span class="option-copy">
                <strong>${option.label}</strong>
                <span>${option.description}</span>
              </span>
            </button>
          `;
        })
        .join("");
    }

    function renderSummary() {
      const stakes = root.querySelector("[data-summary-stakes]");
      const timeline = root.querySelector("[data-summary-timeline]");

      if (stakes) {
        stakes.textContent = state.selections.stakes || "未入力";
      }

      if (timeline) {
        timeline.textContent = state.selections.timeline || "未入力";
      }
    }

    function syncScreens() {
      const step = steps[state.index];
      const isWelcome = step.id === "welcome";
      const isSummary = step.id === "conclusion";

      if (welcomeScreen) {
        welcomeScreen.hidden = !isWelcome;
        welcomeScreen.classList.toggle("is-active", isWelcome);
      }

      if (questionScreen) {
        questionScreen.hidden = isWelcome || isSummary;
        questionScreen.classList.toggle("is-active", !isWelcome && !isSummary);
      }

      if (summaryScreen) {
        summaryScreen.hidden = !isSummary;
        summaryScreen.classList.toggle("is-active", isSummary);
      }

      if (!isWelcome && !isSummary) {
        renderQuestion(step);
      }

      if (isSummary) {
        renderSummary();
      }

      if (nextLabel) {
        nextLabel.textContent = step.action;
      }

      if (backButton) {
        backButton.hidden = state.index === 0;
      }

      if (nextButton) {
        const requiresChoice = Array.isArray(step.options) && questionStepIds.includes(step.id);
        nextButton.disabled = requiresChoice && !state.selections[step.id];
      }

      renderProgress();
    }

    root.addEventListener("click", (event) => {
      const option = event.target.closest("[data-option]");
      if (option) {
        const stepId = option.dataset.step;
        const value = option.dataset.value;

        if (stepId && value) {
          state.selections[stepId] = value;
          syncScreens();
        }
      }
    });

    inputField?.addEventListener("input", (event) => {
      const step = steps[state.index];

      if (!step?.input) return;

      state.selections[step.id] = event.target.value;
      syncScreens();
    });

    nextButton?.addEventListener("click", () => {
      if (state.index === steps.length - 1) {
        state.index = 0;
        state.selections = {};
        syncUrl();
        syncScreens();
        return;
      }

      state.index += 1;
      syncUrl();
      syncScreens();
    });

    backButton?.addEventListener("click", () => {
      state.index = Math.max(0, state.index - 1);
      syncUrl();
      syncScreens();
    });

    window.addEventListener("popstate", () => {
      state.index = getStepIndexFromUrl();
      syncScreens();
    });

    state.index = getStepIndexFromUrl();
    syncUrl(true);
    syncScreens();
  }
</script>

<style>
  :global(:root) {
    color-scheme: light;
    --surface: #f8faf9;
    --surface-bright: #fbfcfb;
    --surface-low: #f1f4f3;
    --surface-high: #e4e9e8;
    --surface-highest: #dde4e3;
    --surface-ghost: rgb(255 255 255 / 0.85);
    --surface-shadow: rgb(45 52 51 / 0.06);
    --text: #2d3433;
    --text-soft: #596060;
    --text-muted: #555d59;
    --primary: #4c645a;
    --primary-dim: #40584e;
    --primary-container: #cde9db;
    --primary-ink: #3f574d;
    --outline-soft: rgb(117 124 123 / 0.16);
    --font-display: "Manrope", "Avenir Next", "Segoe UI", sans-serif;
    --font-body: "Manrope", "Avenir Next", "Segoe UI", sans-serif;
    --font-label: "Inter", "Helvetica Neue", sans-serif;
    --ease-calm: cubic-bezier(0.22, 1, 0.36, 1);
    --radius-default: 0.125rem;
    --radius-lg: 0.25rem;
    --radius-xl: 0.5rem;
    --radius-full: 0.75rem;
  }

  :global(body) {
    margin: 0;
    min-height: 100svh;
    min-height: 100dvh;
    background:
      radial-gradient(circle at top left, rgb(205 233 219 / 0.55), transparent 28%),
      linear-gradient(180deg, #fbfcfb 0%, var(--surface) 40%, #f4f7f6 100%);
    color: var(--text);
    font-family: var(--font-body);
  }

  .decision-app {
    min-height: 100svh;
    min-height: 100dvh;
    padding: 0 1rem calc(6.5rem + env(safe-area-inset-bottom, 0px));
  }

  .app-main,
  .bottom-shell {
    width: min(100%, 52rem);
    margin: 0 auto;
  }

  .app-main {
    padding-top: clamp(2.5rem, 9vw, 5.5rem);
  }

  .screen {
    animation: rise 420ms var(--ease-calm);
  }

  .hero-mark {
    width: 5rem;
    height: 0.3rem;
    border-radius: 999px;
    background: color-mix(in srgb, var(--primary) 18%, transparent);
  }

  .hero-copy {
    display: grid;
    gap: 1.25rem;
    margin-top: 1.75rem;
  }

  .eyebrow {
    margin: 0;
    color: var(--text-muted);
    font-family: var(--font-label);
    font-size: 0.78rem;
    font-weight: 600;
    letter-spacing: 0.14em;
    text-transform: uppercase;
  }

  h1,
  h2 {
    margin: 0;
    font-family: var(--font-display);
    font-size: clamp(2.9rem, 10vw, 4.8rem);
    line-height: 0.98;
    letter-spacing: -0.05em;
    text-wrap: balance;
  }

  h2 {
    font-size: clamp(2.35rem, 8vw, 4rem);
    line-height: 1.02;
  }

  .lede {
    margin: 0;
    max-width: 34rem;
    color: var(--text-soft);
    font-size: clamp(1.05rem, 4vw, 1.35rem);
    line-height: 1.65;
  }

  .supporting-intro {
    margin: 0;
    max-width: 32rem;
    color: var(--text-muted);
    font-size: 1rem;
    line-height: 1.7;
  }

  .example-list {
    display: grid;
    gap: 0.7rem;
    margin: 0;
    padding-left: 1.35rem;
    color: var(--text-soft);
    font-size: 1rem;
    line-height: 1.65;
  }

  .hero-panel,
  .question-panel,
  .summary-panel {
    margin-top: 1.25rem;
    padding: 1.6rem;
    border-radius: var(--radius-xl);
    background: var(--surface-low);
  }

  .supporting-copy,
  .summary-note {
    margin: 0;
    color: var(--text-soft);
    line-height: 1.65;
    font-size: 1rem;
  }

  .screen-intro {
    display: grid;
    gap: 1rem;
  }

  .question-panel,
  .summary-panel {
    display: grid;
    gap: 1rem;
    margin-top: 3rem;
    padding: 1.3rem;
  }

  .question-panel.question-panel-input {
    background: transparent;
    padding: 0;
    gap: 1.1rem;
    margin-top: 2.25rem;
  }

  .editorial-input {
    display: grid;
    gap: 0.8rem;
  }

  .input-label {
    color: var(--text-muted);
    font-family: var(--font-label);
    font-size: 0.82rem;
    font-weight: 700;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    transition: color 320ms var(--ease-calm);
  }

  .editorial-input:focus-within .input-label {
    color: var(--primary);
  }

  .editorial-input-surface {
    position: relative;
    overflow: hidden;
    border-radius: var(--radius-xl);
    background: #eaefee;
    transition:
      background-color 320ms var(--ease-calm),
      box-shadow 320ms var(--ease-calm),
      transform 320ms var(--ease-calm);
  }

  .editorial-input:focus-within .editorial-input-surface {
    background: var(--surface-high);
    box-shadow: 0 12px 36px rgb(45 52 51 / 0.05);
  }

  .editorial-input textarea {
    position: relative;
    z-index: 1;
    width: 100%;
    min-height: 10.75rem;
    box-sizing: border-box;
    padding: 0.85rem 0.95rem 1.1rem;
    border: 0;
    border-radius: 0;
    background: transparent;
    color: var(--text);
    font: inherit;
    font-size: 1.08rem;
    line-height: 1.7;
    resize: vertical;
  }

  .editorial-input textarea::placeholder {
    color: #acb3b2;
  }

  .editorial-input textarea:focus {
    outline: none;
  }

  .editorial-input-accent {
    position: absolute;
    right: 0;
    bottom: 0;
    width: 7rem;
    height: 7rem;
    background: linear-gradient(135deg, transparent 20%, rgb(76 100 90 / 0.08));
    pointer-events: none;
  }

  .option-list {
    display: grid;
    gap: 0.9rem;
  }

  .option-card {
    display: grid;
    grid-template-columns: auto 1fr;
    gap: 1rem;
    align-items: start;
    width: 100%;
    padding: 1.2rem;
    border: 1px solid transparent;
    border-radius: var(--radius-xl);
    background: var(--surface);
    color: inherit;
    text-align: left;
    transition:
      transform 320ms var(--ease-calm),
      background-color 320ms var(--ease-calm),
      border-color 320ms var(--ease-calm);
  }

  .option-card:hover,
  .option-card:focus-visible {
    transform: translateY(-1px);
    outline: none;
    border-color: var(--outline-soft);
    background: var(--surface-bright);
  }

  .option-card.is-selected {
    background: var(--primary-container);
    border-color: color-mix(in srgb, var(--primary) 34%, transparent);
  }

  .option-index {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-width: 2.6rem;
    height: 2.6rem;
    border-radius: 999px;
    background: var(--surface-high);
    color: var(--text-muted);
    font-family: var(--font-label);
    font-size: 0.76rem;
    font-weight: 700;
    letter-spacing: 0.08em;
  }

  .option-card.is-selected .option-index {
    background: rgb(255 255 255 / 0.46);
    color: var(--primary-ink);
  }

  .option-copy {
    display: grid;
    gap: 0.3rem;
  }

  .option-copy strong,
  .summary-card p {
    font-size: 1.02rem;
    line-height: 1.45;
    color: var(--text);
  }

  .option-copy span {
    color: var(--text-soft);
    line-height: 1.6;
  }

  .summary-panel {
    background: transparent;
    padding: 0;
  }

  .summary-card {
    display: grid;
    gap: 0.45rem;
    padding: 1.3rem 1.4rem;
    border-radius: var(--radius-xl);
    background: var(--surface-low);
  }

  .summary-label {
    color: var(--text-muted);
    font-family: var(--font-label);
    font-size: 0.76rem;
    font-weight: 700;
    letter-spacing: 0.12em;
    text-transform: uppercase;
  }

  .summary-card p {
    margin: 0;
  }

  .summary-note {
    padding: 0.25rem 0.25rem 0;
  }

  .summary-question {
    color: var(--text);
    font-family: var(--font-display);
    font-size: 1.1rem;
    font-weight: 700;
    letter-spacing: -0.02em;
  }

  .bottom-bar {
    position: fixed;
    right: 0;
    bottom: 0;
    left: 0;
    z-index: 20;
    padding: 0.75rem 1rem calc(0.9rem + env(safe-area-inset-bottom, 0px));
    pointer-events: none;
  }

  .bottom-shell {
    display: grid;
    grid-template-columns: auto 1fr auto;
    gap: 0.75rem;
    align-items: center;
    padding: 0.65rem;
    border-radius: var(--radius-full);
    background: var(--surface-ghost);
    box-shadow: 0 -8px 40px var(--surface-shadow);
    backdrop-filter: blur(18px);
    pointer-events: auto;
  }

  .progress {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.4rem;
  }

  .progress-dot {
    width: 0.55rem;
    height: 0.55rem;
    border-radius: 999px;
    background: var(--surface-highest);
    transition:
      transform 280ms var(--ease-calm),
      background-color 280ms var(--ease-calm);
  }

  .progress-dot.is-active {
    transform: scale(1.25);
    background: var(--primary);
  }

  .progress-dot.is-complete {
    background: color-mix(in srgb, var(--primary) 38%, var(--surface-highest));
  }

  .primary-action,
  .secondary-action {
    min-height: 3.5rem;
    border: 0;
    border-radius: var(--radius-full);
    font: inherit;
    transition:
      transform 320ms var(--ease-calm),
      opacity 320ms var(--ease-calm),
      background-color 320ms var(--ease-calm);
  }

  .primary-action {
    display: inline-flex;
    align-items: center;
    gap: 0.6rem;
    padding: 0 1.25rem 0 1.45rem;
    background: linear-gradient(135deg, var(--primary) 0%, var(--primary-dim) 100%);
    color: #e5fff2;
    font-family: var(--font-display);
    font-size: 1rem;
    font-weight: 800;
    letter-spacing: -0.02em;
  }

  .primary-action:disabled {
    opacity: 0.45;
  }

  .secondary-action {
    padding: 0 1rem;
    background: color-mix(in srgb, var(--surface-low) 92%, white);
    color: var(--text-soft);
    font-family: var(--font-label);
    font-size: 0.92rem;
    font-weight: 600;
    border-radius: var(--radius-xl);
  }

  .primary-action:not(:disabled):hover,
  .primary-action:not(:disabled):focus-visible,
  .secondary-action:hover,
  .secondary-action:focus-visible {
    transform: translateY(-1px);
    outline: none;
  }

  @media (max-width: 38rem) {
    .decision-app {
      padding-right: 0.75rem;
      padding-left: 0.75rem;
      padding-bottom: calc(6rem + env(safe-area-inset-bottom, 0px));
    }

    .bottom-shell {
      grid-template-columns: 1fr;
      border-radius: var(--radius-full);
      padding: 0.8rem;
    }

    .progress {
      order: -1;
    }

    .secondary-action,
    .primary-action {
      justify-content: center;
      width: 100%;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .screen,
    .option-card,
    .progress-dot,
    .primary-action,
    .secondary-action {
      animation: none;
      transition: none;
    }
  }

  @keyframes rise {
    from {
      opacity: 0;
      transform: translateY(18px);
    }

    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
</style>
